# Normalize line endings: store everything with LF in the repo,
# let Git convert to the platform's native endings on checkout.
* text=auto eol=lf

# Binary assets — never touch these
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
*.db binary
